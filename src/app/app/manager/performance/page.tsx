import ManagerPerformanceDashboard from '@/components/admin/c23-manager-performance-dashboard';

export const metadata = {
    title: 'Performance Review Queue | JamesTronic',
    description: 'Branch-scoped technician performance review — evidence-based, no rankings',
};

export default function ManagerPerformancePage() {
    return <ManagerPerformanceDashboard />;
}
